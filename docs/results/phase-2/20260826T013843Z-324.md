# Phase 2 Bounded Mission/Action Result: `20260826T013843Z-324`

The bounded Phase 2 headless verifier exited successfully on 2026-08-26. This
is local development evidence, not release or benchmark evidence: the run was
made from Git commit `492f84bf9b9dfac32ff1145b2aa88a66f58a9de1` with a dirty
worktree. It verifies the Phase 2 mission/action integration subset and global
resource/isolation gates. It does not establish full Scenario 1 acceptance.

## Reproduce the automated gate

```powershell
wsl -d Ubuntu -- bash -lc 'cd /home/hasan/robotest-lab && ROBOTEST_SIM_SEED=42 ROBOTEST_CPUSET=0-5 scripts/verify_phase2.sh'
```

The run recorded original script argv `scripts/verify_phase2.sh`, the guarded
runtime affinity `0-5` with six inherited logical CPUs, source/configuration
hashes, Git state, dependency versions, ROS domain `194`, and Gazebo partition
`robotest_phase2_20260826T013843Z_324`. The launch, runtime probe, and mission
runner child argv and working directories were also captured. The simulator
seed was `42`; the Phase 2 baseline fault schedule was empty and fault arming
was `NOT_IMPLEMENTED_NOT_EXERCISED`.

Compact tracked projections are available as
[JSON](20260826T013843Z-324.json) and [CSV](20260826T013843Z-324.csv). The JSON
is byte-identical to the canonical run-local result. The CSV contains exactly
one semantically matching row and differs from the raw CSV only by LF line-end
normalization.

## Automated result

| Gate | Target | Observation | Result |
| --- | --- | --- | --- |
| Process and evidence | Exit zero; finalized valid evidence | Verifier, tee, mission, runtime probe, launch-log scan, and checksum validation exited zero; all 112 checksum entries validated | Pass |
| Deterministic input | Fixed simulator seed and isolated runtime | Seed `42`, CPUs `0-5`, ROS domain `194`, unique Gazebo partition, empty fault schedule | Pass |
| Mission/action | Three ordered waypoints; terminal success; no missed waypoint | Nav2 `SUCCEEDED`; 3/3 completed; error code 0; 986 valid feedback observations | Pass |
| Bounded feedback trace | Capacity known; no overflow or invalid entry | `986 / 4096` observations retained; overflow `false`; overflow count `0`; invalid index count `0` | Pass |
| Mission timeout | At most 180 simulation seconds and 300 wall seconds | `95.568 s` simulation completion; `98.469194891 s` accepted-to-terminal wall duration | Pass |
| Action ownership | One expected client and server | `/robotest/mission_runner` client; `/robotest/waypoint_follower` server; exact `FollowWaypoints` type | Pass |
| Command and motion correlation | Connected three-stage command trace; nonzero command and ground-truth motion | 940, 942, and 939 nonzero in-interval command samples; 1,912 ground-truth samples covered `7.3710067540 m` | Pass |
| Nav2 readiness | Required lifecycle nodes active; valid map, costmaps, TF, and simulation time | Startup, graph, parameters, costmaps, runtime probe, and lifecycle gates passed | Pass |
| Tests | No errors or failures; fresh retained fixture proof | 218 tests, 0 errors, 0 failures, 10 cppcheck-version skips; current-run `pytest.xml` retained 70 mission cases | Pass with stated skip |
| Peak owned-process RSS | At most `6,291,456 KiB` | `1,378,196 KiB` | Pass |
| Calculated RTF median | At least `0.80` | `0.9820623137624128` | Pass |
| Calculated RTF p5 | At least `0.50` | `0.9084674490661722` | Pass |

The ten skips are package-level `ament_cppcheck` checks that decline cppcheck
2.13 because of its documented performance issue. They are version-policy
skips, not failures. The verifier proved that the retained 70-case mission
`pytest.xml` was generated in this run from the current source, and all 70
discovered cases passed. Its 16 exact required fixtures covered bounded clock
readiness, frozen-clock wall escape, cancellation acknowledgement/result,
UUID-matched status T0, immutable action-status timestamps, response/T0
mismatch, late status conflict, status QoS, and bounded feedback overflow.

The calculated RTF used 213 non-overlapping five-sample windows over 1,070
retained world-statistics samples, without trace drops. Gazebo's separately
retained reported RTF had median `0.99901222668837` and p5
`0.6049806239830654`; it is secondary evidence and was not substituted for the
calculated gate. Resource sampling covered the recorded launch, runtime-probe,
and mission-runner process groups.

## Mission and authoritative action time

The accepted goal UUID was
`79047003-634f-41a0-86b3-aa7294031fb7`. The recorded time sequence was:

| Observation | Simulation timestamp | Semantics |
| --- | ---: | --- |
| Client goal submission | `69.106 s` | Positive client-local ordering evidence |
| Raw `SendGoal` response stamp | `0 s` | Explicitly unavailable for this Jazzy `rclcpp_action` server; not T0 |
| Accepted-goal T0 | `69.106 s` | Positive immutable `goal_info.stamp` from the exact UUID-matched action-status entry |
| Terminal result observation | `164.674 s` | Client observation of terminal `SUCCEEDED` result |

The authoritative accepted-goal stamp source was
`uuid_matched_action_status_goal_info`. The mission reached action status code
`4` (`SUCCEEDED`) with Nav2 error code `0`, no error message, no missed
waypoint, and the ordered transition sequence `[0, 1, 2]`. Completion took
`95.568 s` of simulation time; the mission runner's wall duration was
`98.611254540 s`.

All 986 feedback observations were retained within the fixed 4,096-entry
capacity. Overflow was `false`, overflow count was `0`, and invalid index count
was `0`. Feedback timestamps were checked against the client
submission-to-terminal interval, while the UUID-matched server action-status
stamp remained the authoritative T0. The canonical mission JSON and its
one-row CSV reconciled exactly; action trace and mission/runtime correlation
both passed.

## Command chain, motion, QoS, TF, and isolation

The live command ownership matched the Phase 2 contract:

```text
controller_server -> cmd_vel_nav
velocity_smoother -> cmd_vel_smoothed
collision_monitor -> cmd_vel -> Gazebo bridge
```

Within the authoritative T0-to-terminal interval, `cmd_vel_nav`,
`cmd_vel_smoothed`, and `cmd_vel` retained 944, 958, and 955 samples,
respectively; 940, 942, and 939 were nonzero. The ground-truth interval
contained 1,912 samples and had a `7.3710067540499375 m` bounding span.
Correlation passed without trace overflow.

Live reliability and durability were compatible at each inspected endpoint;
the command chain was reliable and volatile. Fast DDS on Jazzy reported
`history=UNKNOWN` and `depth=0`, so live bounded-depth proof remained
unavailable. The verifier retained exact static bounded-queue contracts and
tests and did not turn unknown live introspection into a depth claim.

Required dynamic and static TF edges, endpoint sets, and ownership constraints
passed. Jazzy `rclpy` callbacks do not expose publisher GIDs, so the run cannot
attribute every received TF edge to an individual live publisher. Validation
topics remained isolated from autonomy consumers, and costmap service probes
returned valid positive-stamp global and local maps.

## Rendering and environment

The fresh post-launch OGRE log captured a real content delta and passed its
freshness gate. It records OGRE-Next using OpenGL 3+ through
`llvmpipe (LLVM 20.1.2, 256 bits)`. This is successful headless software
rendering, not hardware-GPU evidence. No Phase 2 RViz or visual acceptance is
claimed by this run.

## Evidence integrity

The `SHA256SUMS` manifest contains 112 entries. The persisted validation
sidecar contains 112 matching `OK` lines with no failure or warning, and the
manifest SHA-256 is
`3dfd0ff2e1fdee53341d770c8a40070879ff6ed893b84e41b7fdbd88898d41b6`.

| Evidence | Run-local SHA-256 |
| --- | --- |
| `run-result.json` | `c6062ce40dd513d135b17ca0887c4c26c791375aea57d434a93f9710579543d7` |
| `run-result.csv` | `3b4fb19ba8a3ededcabd5bc4a3a43dfe5c1ccd5b96e03cd4b979691e723fc839` |
| `mission-result.json` | `b02628360df4a858248fbb2e39328d4dd424b64f5e46cae2078a006591fe17a4` |
| `action-trace.json` | `39c8ee1d0b667e04eea4349b4cf88c4bc9ba7db08fea531c9a58d68cd444979a` |
| `mission-runtime-correlation.json` | `7ac4316fff16fc372bd23f1912a44bfd5e194833e15ad7b16fcd4350c2635a6e` |
| `runtime-probe.json` | `4fc2b9e352c4c524d788c4687662f5edc62ea321ac0f6be593349d8fb5448e8d` |
| `resources.csv` | `fae5bec51e4180c37e70e91ec23b3d6655bf85b066bc3d82af536dff0d9847f5` |
| `provenance.json` | `c73d35a88f758f7a5c95ca0ac7e8a264a2f7d3d0186545aaa37753b27d10a4c7` |
| `robotest-missions-pytest.xml` | `09d75cdd1b937e6393860d773ca87b8174768892a987f21d908189158dda353e` |
| `installed-source-binding.json` | `a434c41c6502f125d13ce0de8bda4a723dc076e89b49453e944a26e07fffaed6` |
| `source-config-mutation.json` | `e215610d4bbf3ee45869a145cc976e9fb88fe340749a642734312bdf5ff30b9d` |
| `process-cleanup.json` | `47b92e08d1d576d9cc18dc227df226bd93d68f540d5187043bbda60870fdff5d` |
| `verify.log` | `380674b10659616c404e9b165ba117b7162c92301a210c9fa74a374f605a67a5` |
| `summary.json` | `e3d4bfd5f48be55b51923ab130ff687782bdc3a03fdc43758b4d76c8654dc974` |

The start and end aggregate hash for all 93 source/configuration-bound files
was unchanged at
`8c73bfdd0c44d2e9127abea3fe61e5aa26a1c1b12182eccca6db09fc2e9e56b1`:
no file was added, changed, or removed during the run. All 20
source/install bindings matched. The verifier cleaned only its three recorded
process groups, and the cleanup artifact reported no remaining process in
those groups. Evidence was finalized at `2026-08-26T01:42:02Z`.

The tracked JSON is byte-identical to the raw run-local JSON, with SHA-256
`c6062ce40dd513d135b17ca0887c4c26c791375aea57d434a93f9710579543d7`.
The raw CSV uses CRLF and has SHA-256
`3b4fb19ba8a3ededcabd5bc4a3a43dfe5c1ccd5b96e03cd4b979691e723fc839`;
the tracked CSV differs only by LF normalization and has SHA-256
`f142bae6400875ac15a4c09fdab5c111d9108ef68f2a81bf0ac73fe349f6a050`.

## Scope and limitations

This result verifies a bounded, seeded, headless Phase 2 Nav2 mission/action
integration and its global resource, isolation, ownership, timestamp,
feedback-bounds, and evidence-integrity gates. It is not a full-project or
full-Scenario 1 result.

- The Git worktree was dirty, so the run is development evidence and not a
  release benchmark or clean-commit reproducibility result.
- Fault preload/arming, injected-fault behavior, recovery, and repeatability
  remain Phase 3 or later work.
- `collision_count`, actual path length, and path efficiency are null. Collision
  absence and path-efficiency acceptance are not established.
- Full Scenario 1 acceptance and its required repeated trials remain
  `NOT_EVALUATED_PHASE3`/`DEFERRED_TO_PHASE3`.
- Live Fast DDS history/depth introspection remained `UNKNOWN`/`0`; only the
  static bounded-depth proof is claimed.
- TF endpoint owners and required edges were observed separately, without live
  per-edge publisher-GID attribution.
- Rendering used `llvmpipe`; hardware acceleration was not tested.
