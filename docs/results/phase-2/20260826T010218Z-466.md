# Phase 2 Bounded Mission/Action Result: `20260826T010218Z-466`

The bounded Phase 2 headless verifier exited successfully on 2026-08-26. This
is local development evidence, not release or benchmark evidence: the run was
made from Git commit `492f84bf9b9dfac32ff1145b2aa88a66f58a9de1` with a dirty
worktree. It verifies the Phase 2 mission/action integration subset and global
resource/isolation gates. It does not establish full Scenario 1 acceptance.

## Reproduce the automated gate

```powershell
wsl -d Ubuntu -- bash -lc 'cd /home/hasan/robotest-lab && ROBOTEST_SIM_SEED=42 ROBOTEST_CPUSET=0-5 scripts/verify_phase2.sh'
```

The run recorded original script argv `scripts/verify_phase2.sh`, the guarded
runtime affinity `0-5` with six inherited logical CPUs, source and configuration
hashes, Git state, dependency versions, ROS domain `196`, and Gazebo partition
`robotest_phase2_20260826T010218Z_466`. The launch, runtime probe, and mission
runner child argv and working directories were also captured. The scenario and
simulator seed were both `42`; the fault schedule was empty and fault arming was
`NOT_IMPLEMENTED_NOT_EXERCISED`.

Compact tracked projections are available as
[JSON](20260826T010218Z-466.json) and [CSV](20260826T010218Z-466.csv). The CSV
contains exactly one row and matches the scalar projection of the JSON.

## Automated result

| Gate | Target | Observation | Result |
| --- | --- | --- | --- |
| Process and evidence | Exit zero; finalized valid evidence | Verifier, tee, mission, runtime probe, launch-log scan, and checksum validation exited zero; all 111 checksum entries validated | Pass |
| Deterministic input | Fixed simulator seed and isolated runtime | Seed `42`, CPUs `0-5`, ROS domain `196`, unique Gazebo partition, empty fault schedule | Pass |
| Mission/action | Three ordered waypoints; terminal success; no missed waypoint | Nav2 `SUCCEEDED`; 3/3 completed; error code 0; 906 valid feedback observations | Pass |
| Mission timeout | At most 180 simulation seconds and 300 wall seconds | `88.686 s` simulation completion; `90.503948748 s` accepted-to-terminal wall duration | Pass |
| Action ownership | One expected client and server | `/robotest/mission_runner` client; `/robotest/waypoint_follower` server; exact `FollowWaypoints` type | Pass |
| Command and motion correlation | Connected three-stage command trace; nonzero motion and final zero | 862, 858, and 858 nonzero in-interval samples; all three stages ended at zero; 1,774 ground-truth samples covered `7.6437868254 m` | Pass |
| Nav2 readiness | Required lifecycle nodes active; valid map, costmaps, TF, and simulation time | Nine lifecycle nodes active; collision monitor active before and at mission start; checked participants used simulation time | Pass |
| Tests | No errors or failures | 213 tests, 0 errors, 0 failures, 10 cppcheck-version skips | Pass with stated skip |
| Peak owned-process RSS | At most `6,291,456 KiB` | `1,372,244 KiB` | Pass |
| Calculated RTF median | At least `0.80` | `0.9863029171557808` | Pass |
| Calculated RTF p5 | At least `0.50` | `0.9302367113894893` | Pass |

The ten skips are package-level `ament_cppcheck` checks that decline cppcheck
2.13 because of its documented performance issue. They are version-policy
skips, not failures. Fresh current-run fixture evidence includes the bounded
clock-readiness, frozen-clock wall escape, cancellation acknowledgement/result,
zero raw response stamp, UUID-matched status T0, immutable status stamp,
response/T0 mismatch, late status conflict, and status-QoS cases.

The calculated RTF uses 202 non-overlapping windows over 1,011 retained
world-statistics samples. Gazebo's separately retained reported RTF had median
`0.9994143431948878` and p5 `0.6446671841195265`; it is secondary evidence and
was not substituted for the calculated gate. Resource sampling covered the
recorded launch, runtime-probe, and mission-runner process groups.

## Mission and authoritative action time

The accepted goal UUID was
`e7006620-847c-4994-a00c-60581ddd1cd0`. The recorded time sequence was:

| Observation | Simulation timestamp | Semantics |
| --- | ---: | --- |
| Client goal submission | `68.278 s` | Positive client-local ordering evidence |
| Raw `SendGoal` response stamp | `0 s` | Explicitly unavailable for this Jazzy `rclcpp_action` server; not T0 |
| Accepted-goal T0 | `68.308 s` | Positive immutable `goal_info.stamp` from the exact UUID-matched action-status entry |
| Terminal result observation | `156.994 s` | Client observation of terminal `SUCCEEDED` result |

The mission runner persisted
`accepted_goal_stamp_source=uuid_matched_action_status_goal_info`. It required
two positive advancing clock observations before dispatch, correlated the
client clock to T0, and continued validating the immutable exact-UUID status
stamp through terminal receipt. The 906 feedback observations were monotonic,
had zero invalid indices, followed the ordered transition sequence `[0, 1, 2]`,
and lay within the client submission-to-terminal interval. Feedback callback
timestamps are client-clock observations and are not misrepresented as
server-status timestamps.

The mission JSON and one-row CSV reconciled exactly. The action trace and the
separate mission/runtime-correlation artifact reported no failures.

## Command chain, motion, QoS, TF, and isolation

The live command ownership matched the Phase 2 contract:

```text
controller_server -> cmd_vel_nav
velocity_smoother -> cmd_vel_smoothed
collision_monitor -> cmd_vel -> Gazebo bridge
```

`cmd_vel_behavior_unused` had no subscriber. Across the authoritative
T0-to-terminal interval, `cmd_vel_nav`, `cmd_vel_smoothed`, and `cmd_vel`
retained 866, 876, and 876 samples respectively, without trace drops. Every
stage had nonzero motion and ended within the zero-command tolerance. The
ground-truth interval began near `(0, -3.5)` and ended near
`(0.16985, 3.63198)` with zero final linear and angular velocity.

Live reliability and durability were compatible at each inspected endpoint;
the command chain was reliable and volatile. Fast DDS on Jazzy reported
`history=UNKNOWN` and `depth=0`, so live bounded-depth proof remained false.
The verifier retained exact static bounded-queue contracts from project source
and tests, found no static/live mismatch, and did not convert unknown values
into a false live claim.

Observed dynamic TF included `map -> odom` and `odom -> base_footprint`, plus
the wheel transforms. The expected static robot chain was present. The probe
found no duplicate edge within a message, no dynamic/static overlap, and no
multiple-parent child. Jazzy `rclpy` callbacks do not expose publisher GIDs, so
the run cannot attribute each received TF edge to one live publisher; endpoint
sets and static source/test ownership are separate evidence.

Only the evidence runtime probe subscribed to validation contacts, ground
truth, and world-statistics topics. No autonomy validation subscriber was
observed. Both Nav2 costmap service probes returned positive stamps, nonempty
geometry, the expected frames, and data sizes matching their geometry.

## Rendering and environment

The fresh post-launch OGRE log captured a real content delta and passed its
freshness gate. It records OGRE-Next using OpenGL 3+ through
`llvmpipe (LLVM 20.1.2, 256 bits)`. This is successful headless software
rendering, not hardware-GPU evidence. No Phase 2 RViz or visual acceptance is
claimed by this run.

## Evidence integrity and independent review

The `SHA256SUMS` manifest contains 111 entries, and the persisted validation
sidecar contains 111 matching `OK` lines with no failure or warning. The
manifest SHA-256 is
`42c97161b75511f360bfe52c6432e3e9df2b86843213f40beb4d8371d0c09fb1`.

| Evidence | Run-local SHA-256 |
| --- | --- |
| `run-result.json` | `10cd4bac7fc1917557b0f01c62c70f1a3ddbfda877b5de33013615e1c1943444` |
| `run-result.csv` | `48f4c247656bd8361384bf6d17e0dea149e85882826eb25ba4fbefc7a86a7843` |
| `mission-result.json` | `8c25b4a516c6defeb7429b1188eb506f2574c660c5fa287b4f82a36d26dc9c33` |
| `action-trace.json` | `5f165a2789dcbbe3938f714e5b42fe804b941766985aba456d8de0fb9d3687b1` |
| `mission-runtime-correlation.json` | `2740ca27193436fc239c5101a18d9f4669b895bdeb776526094cf28e7fc06995` |
| `runtime-probe.json` | `9991d561b8e4f7dc03d3b135671703ce65a922f2e160dd954d745b86b21d469e` |
| `resources.csv` | `11b3ae647a3fe15a6dca7dffcd5579276c690cd3ba4160f41bc8a8b33e338449` |
| `provenance.json` | `81d22579a09b9f76a786a786aa4ce348f6b8464872994b099e35b8ffb5e6a44b` |
| `installed-source-binding.json` | `48ffe3ae030aa8141152114ec6de654c9407299a8af8bd29b7571f0d6fadaba4` |
| `source-config-mutation.json` | `b8ac6939f6f8a3a6c24c34a5bb724e66d9ab58de6e34cf0c7c78f3fc1907174e` |
| `process-cleanup.json` | `2b094b4d81711df6903abfa0cb67e61ea2d8507738573fe6c364c55658295a55` |
| `verify.log` | `2a9b5f731ea775878af0c5551aad073c3d1360673e5487b5f9476e34e1db1010` |
| `summary.json` | `d82fac733c15f2bf8440190d5ed76e3f08df1cc1d4e405cf75dba9c7aa90b9a2` |

The start and end aggregate source/configuration hash was unchanged at
`b1cb2fbbb12585d6dec8963af77dbc59f1fb17329486f5027ce2cdb0c18b16bb`.
All 17 source/install bindings matched. The verifier cleaned only its three
recorded process groups; run evidence and an independent post-run audit found
no remaining process in those groups. The independent audit also reconciled
mission stamps, ordered feedback, action ownership, interval-filtered command
and ground-truth traces, tests/skips, checksums, and current exact
RoboTest/Gazebo/ROS-daemon process identifiers without finding a blocker.

The tracked compact JSON is byte-identical to the raw run-local JSON, with
SHA-256
`10cd4bac7fc1917557b0f01c62c70f1a3ddbfda877b5de33013615e1c1943444`.
The raw CSV uses CRLF and has SHA-256
`48f4c247656bd8361384bf6d17e0dea149e85882826eb25ba4fbefc7a86a7843`;
the tracked CSV differs only by LF normalization and has SHA-256
`a658d02de9626b1207e60dcb809c5e67e982951277da8a0a339391a5db8fb4d7`.

## Scope and limitations

This result verifies a bounded, seeded, headless Phase 2 Nav2 mission/action
integration and its global resource, isolation, ownership, timestamp, and
evidence-integrity gates. It is not a full-project or full-Scenario 1 result.

- The Git worktree was dirty, so the run is development evidence and not a
  release benchmark or clean-commit reproducibility result.
- Fault preload/arming, injected-fault behavior, recovery, and repeatability
  remain Phase 3 or later work.
- `collision_count`, actual path length, and path efficiency are null. The
  contacts endpoint existed but supplied no collision metric, so collision
  absence is not established.
- Full Scenario 1 acceptance and its required repeated trials remain
  `NOT_EVALUATED_PHASE3`/`DEFERRED_TO_PHASE3`.
- Live Fast DDS history/depth introspection remained `UNKNOWN`/`0`; only the
  static bounded-depth proof is claimed.
- TF endpoint owners and required edges were observed separately, without live
  per-edge publisher-GID attribution.
- Rendering used `llvmpipe`; hardware acceleration was not tested.
